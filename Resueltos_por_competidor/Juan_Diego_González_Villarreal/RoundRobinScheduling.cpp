#include<bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    int n, cont, inda,dif, indb, it;
    cin>>n;
    vector<ll> listaT(n), temp(n);
    for (int i = 0; i<n; i++) {
        cin>>listaT[i];
        temp[i] = listaT[i];
    }


}
